[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mypackage"
version = "0.1.0"
description = "A simple Python package for demonstration"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = ["pytest>=7.4.0"]
